export default function Home() {
  return (
    <section className="text-center w-full">
      <h1 className="text-5xl sm:text-6xl font-extrabold tracking-tight text-[var(--primary)]">
        traff-29
      </h1>
      <p className="mt-4 text-lg sm:text-xl text-[var(--text-secondary)]">
        turning chaos into motion
      </p>
      {/* Minimal hero only – no entry animation */}
    </section>
  );
}
